import {ActionsTypes, StateType,} from "./Store";

let initialState = {}

const sidebarReducer = (state = initialState /*: StateType*/, action: ActionsTypes) => {

    return state
}

export default sidebarReducer